# Optional: only needed when running the Express/Gemini backend with npm run dev:server.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Optional: URL where a custom server deployment is hosted.
APP_URL="MY_APP_URL"
